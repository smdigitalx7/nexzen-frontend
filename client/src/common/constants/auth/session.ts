/** Total inactivity time before automatic logout (30 minutes). */
export const IDLE_TIMEOUT_MS = 15 * 60 * 1000;

/** Warning shown this long before idle logout (5 minutes). */
export const IDLE_WARNING_MS = 2 * 60 * 1000;
